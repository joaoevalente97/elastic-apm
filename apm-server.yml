output.elasticsearch:
    hosts: ["elasticsearch:9200"]
    username: elastic
    password: bitnami

apm-server.kibana.enabled: true
apm-server.kibana.host: "kibana:5601"

apm-server.host: minideb:8200
